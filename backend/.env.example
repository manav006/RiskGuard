PORT=8000

# Razorpay test API keys (placeholders only — leave empty until real test keys are
# available). When both are present, the backend automatically switches the
# /api/razorpay/test-payments endpoint from mock data to live Razorpay data.
RAZORPAY_KEY_ID=your-test-key-id
RAZORPAY_KEY_SECRET=your-test-key-secret
